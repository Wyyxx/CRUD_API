const app = require("express");
const server = app();


server.put("/:valor", (req,res) =>{

})

server.post("/:valor:index", (req,res) =>{

})

server.delete("/:index", (req,res) =>{

})

server.get("/:index",(req,res) =>{

})
server.listen(3001,() => {
    console.log("Server is running")
    })